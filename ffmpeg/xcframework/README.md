Nothing here is source: these are FFmpeg builds for phones, kept so an app can embed them
and so anyone can replace them with their own build — which is what the licence asks.

Built by ./build-mobile.sh and packaged by ./package-apple.sh, from FFmpeg n9.0.1, LGPL,
no --enable-gpl and no x264/x265.
